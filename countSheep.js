var countSheep = function (num){
    let str = "";
     for(let i = 1; i <= num; i++) { str+= `${i} sheep...`; }
     return str;
   }
   
   //First, set a variable as an empty string.
   
   //Second, write a for loop with num as the other variable.
   
   //Third, after each revolution of the loop have the string add 1(+=) to the string with backticks
   //{i} sheep...
   
   //Fourth, return string variable